/dist/
/.gocache/
/.gomodcache/
__debug*
.claude
server/h3studio
docs/tasks/*
.DS_Store

# helm dev keeps the studio's whole tree here, sessions included, and
# scripts/run.sh keeps its pid file and the debugger's shim beside it.
/.helm/
/.cache/
