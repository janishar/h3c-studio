sessions/**
!sessions/.keep
/dist/
/.gocache/
/.gomodcache/
__debug*
.claude
server/h3studio
docs/tasks/*
.DS_Store

# helm dev keeps the studio's whole tree here when it runs standalone, and
# scripts/dev.sh keeps its pid file beside it.
/.helm/
/.cache/
